
deployment:
  tasks:
    - export DEPLOYPATH=/home/teketibk/staging.tekete.co.za
    - /bin/cp -R . $DEPLOYPATH
    - cd $DEPLOYPATH && $PHP artisan migrate --force
    - cd $DEPLOYPATH && $PHP artisan optimize:clear
    
